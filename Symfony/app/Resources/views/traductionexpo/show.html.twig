{% extends 'base.html.twig' %}

{% block body %}
    <h1>TraductionExpo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ traductionExpo.id }}</td>
            </tr>
            <tr>
                <th>IdLangue</th>
                <td>{{ traductionExpo.idLangue }}</td>
            </tr>
            <tr>
                <th>IdExpo</th>
                <td>{{ traductionExpo.idExpo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('traductionexpo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('traductionexpo_edit', { 'id': traductionExpo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
